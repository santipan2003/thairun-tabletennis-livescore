import { NextPage } from "next";
import Score from "./components/Score";

export const ScoreInMatch: NextPage = () => {
  return (
      <div>
        <Score />
      </div>
  );
};

export default ScoreInMatch;
